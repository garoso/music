<div class="container mt-3">
  <div class="row">
    <div class="col-sm">

        <% @artists.each do |artist|%>
        <div class="col-sm-3 d-inline-block">
          <div class="card p-2 m-1 mr-1">
            <img class="card-img-top" style="height: 250px;" src="<%= artist.image_url%>">
            <div class="card-body">
              <h5 class="card-title text-center"><%= artist.name.capitalize %></h5>
              <%= link_to "Ver más",  artist_path(artist), class: "btn btn-primary col-12" %>
            </div>
          </div>
        </div>
        <% end %>
    </div>
  </div>
</div>
